function LoginButton({ loginSignup }) {
    return (
        <div className="loginSignup-btn">
            <button type="button">{loginSignup}</button>
        </div>
    );
}

export default LoginButton;
